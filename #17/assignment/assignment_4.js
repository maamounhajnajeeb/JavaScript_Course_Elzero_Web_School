let childrenBody = document.body.children;
let divElement = childrenBody[1];
let elzeroText = divElement.innerText;

console.log(elzeroText.split(" ")[1]);
console.log(elzeroText.split(" ")[1].length);