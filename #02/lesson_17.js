let title = "Elzero",
    paragraph = "Elzero Web School",
    date="25/10";

let card_info = `
<div>
    <h2>Hello ${title}</h2>
    <p>${paragraph}</p>
    <span>${date}</span>
</div>
`;

document.write(card_info);
document.write(card_info);
document.write(card_info);
document.write(card_info);
